apiVersion: v1
kind: Service
metadata:
  name: {{ include "javascript-pnpm-react.fullname" . }}
  labels:
    {{- include "javascript-pnpm-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javascript-pnpm-react.selectorLabels" . | nindent 4 }}
