apiVersion: v1
kind: Service
metadata:
  name: {{ include "nm-javascript-npm-react.fullname" . }}
  labels:
    {{- include "nm-javascript-npm-react.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nm-javascript-npm-react.selectorLabels" . | nindent 4 }}
