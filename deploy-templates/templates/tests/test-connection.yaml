apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-pnpm-react.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-pnpm-react.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-pnpm-react.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
