apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-import-def-github.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-import-def-github.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-import-def-github.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
